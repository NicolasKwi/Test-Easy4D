{% extends 'base.html.twig' %}

{% block title %}Edit Produit{% endblock %}

{% block body %}
    <div class="container mt-4 mx-auto">
        <h1 class="text-center text-decoration-underline mb-5">Modifier le Produit</h1>

        {{ include('produit/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <a href="{{ path('app_produit_index') }}"  class="btn btn-secondary m-1">Retour à la liste</a>

        {{ include('produit/_delete_form.html.twig') }}
    </div>
{% endblock %}
